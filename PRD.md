# PRD — 인사·총무 교육용 가상 업무 앱

> (주)다온에프에스(DAON-FS) 교육용 가상 시스템. 모든 인물·데이터는 창작된 가상 자료이며 실제 인사·급여 정보가 아닙니다.

## 1. 목적

- 인사(HR)·총무(GA) 업무 시스템을 처음 쓰는 **전 직원**이, 실제 화면과 비슷한 가상 환경에서 미리 사용법을 익히도록 돕는다.
- 실제 회사/인물/문서는 전혀 사용하지 않고, 처음부터 만든 가상 사례만 사용한다.
- 모든 화면·파일에서 "교육용 가상 자료"임을 명확히 표시해 실제 정보로 오인되지 않게 한다.

## 2. 사용자 역할

로그인 시 이름·사번(가상)을 입력하고 아래 3가지 역할 중 하나를 선택한다. 역할에 따라 보이는 메뉴/기능이 다르다.

| 역할 | 설명 |
|---|---|
| 일반직원 | 본인 조회 + 본인 신청(휴가/비품/회의실)만 가능 |
| 인사담당자 | 일반직원 기능 + 전체 휴가 승인, 사원 목록 조회 |
| 총무담당자 | 일반직원 기능 + 전체 비품 승인, 공지 작성 |

## 3. 기능 (화면 목록)

| 화면 파일 | 접근 역할 | 설명 |
|---|---|---|
| `index.html` | 전체 | 진입점. 브라우저 표준 규칙상 서버 루트(`/`)나 폴더를 열었을 때 자동으로 열리는 파일명이라, 더블클릭/서버 접속 즉시 `training_index.html`로 이동시키는 리다이렉트 전용 페이지 (콘텐츠는 없음 — 그래서 `training_` 접두어를 붙이지 않음) |
| `training_index.html` | 전체 | 로그인 (이름/사번 입력 + 역할 선택) |
| `training_dashboard.html` | 전체 | 대시보드 (내 신청 현황, 최근 공지, 역할별 처리 대기 안내) |
| `training_hr_myinfo.html` | 전체 | 내 정보 조회 (사원정보·연차 현황·이번 달 급여 요약) |
| `training_hr_leave.html` | 전체 | 휴가 신청 + 내 신청 내역 조회 |
| `training_hr_leave_approve.html` | 인사담당자 | 전체 휴가 신청 승인/반려 처리 |
| `training_hr_employees.html` | 인사담당자 | 전체 사원 목록 조회 |
| `training_ga_supply.html` | 전체 | 비품/소모품 신청 + 내 신청 내역 조회 |
| `training_ga_supply_approve.html` | 총무담당자 | 전체 비품 신청 승인/반려 처리 |
| `training_ga_room.html` | 전체 | 회의실 예약 + 전체 예약 현황 조회 (중복 예약 체크 포함) |
| `training_ga_notice.html` | 전체(작성은 총무담당자) | 사내 공지 조회 / 작성 |

권한 없는 화면에 URL로 직접 접근하면 안내 후 대시보드로 자동 이동한다 (UI 수준의 접근 제어, 실제 보안 아님 — 교육용이므로 의도된 설계).

## 4. 데이터 규칙

세 가지 자료(직원/휴가/급여)를 각각 표로 나누고, **사번(empNo)을 공통 연결 키**로 사용한다. 자세한 항목 설명은 [`데이터안내.md`](./데이터안내.md) 참고.

- **원본 데이터**: `data/가상_인사총무.json` (직원 12명 · 휴가 6건 · 급여 12건)
- **앱 내 실제 사용 데이터**: `assets/training_seed_data.js`에 위 JSON과 동일한 내용을 직접 적어 넣음. 크롬 등 브라우저가 `file://` 페이지에서 JSON을 fetch로 불러오는 것을 막기 때문에, 더블클릭 실행 호환성을 위해 fetch 대신 JS 변수로 내장한다.
- **버전 관리**: `TRAINING_SEED_VERSION` 값을 바꾸면 브라우저에 저장된 이전 데이터를 새 구조로 다시 채운다 (스키마가 바뀔 때마다 버전을 올릴 것).
- **연차 반영 규칙**: 휴가 상태가 "승인"인 건만 해당 직원의 사용 연차에 반영한다. 대기/반려 건은 차감하지 않는다.
- **급여 접근 범위**: 급여는 로그인한 본인 것만 조회 가능하다. 인사담당자도 다른 사람의 급여는 볼 수 없다 (전체 급여 조회 화면 없음 — 민감 정보라 범위를 좁게 유지하기로 결정).

### 4-1. 로그인 계정과 가상 사원 자료 연결 기준 (목표 규칙)

- 로그인 화면은 이름과 사번을 모두 입력받는다.
- **연결 매칭은 사번(empNo) 기준으로 한다.** 이름은 동명이인이 있을 수 있어 연결 키로 쓰지 않는다. 사번이 시드 사원 목록(`data/가상_인사총무.json`)과 일치하면, 내 정보/연차/급여 화면에 해당 인물의 실제 데이터가 표시된다. 일치하지 않으면 기본값(가상 프로필)으로 표시한다.
- **적용 완료**: `training_app.js`(`trainingFindMyEmployee`), `training_hr_myinfo.html`, `training_hr_leave.html`, `training_ga_supply.html`, `training_dashboard.html`을 이름 매칭 → 사번 매칭으로 전환했다. 이름을 틀리게 입력해도 사번만 맞으면 정상 연결됨을 확인했다.

## 5. 확인 기준 (Acceptance Criteria)

이번 세션에서 Playwright로 실제 화면을 조작해 아래 시나리오를 확인했다. 앞으로 기능을 바꿀 때도 이 시나리오들이 계속 통과해야 한다.

1. **역할별 메뉴 노출**: 일반직원으로 로그인하면 "휴가 승인 처리", "사원 목록", "비품 승인 처리" 메뉴가 보이지 않는다.
2. **휴가 신청 → 저장**: 일반직원이 휴가를 신청하면 "대기" 상태로 저장되고, 본인의 신청 내역 목록에 바로 나타난다.
3. **승인 처리 → 상태 반영**: 인사담당자가 휴가 승인 화면에서 대기 건을 "승인" 처리하면, 상태가 즉시 "승인"으로 바뀌고 처리 버튼이 사라진다.
4. **권한 없는 페이지 접근 차단**: 일반직원/인사담당자가 자신의 역할에 없는 전용 화면(예: 총무 승인 화면)에 URL로 직접 들어가면, 안내 메시지 후 대시보드로 자동 이동한다.
5. **실데이터 연결**: 가상 사원 명단에 있는 이름/사번으로 로그인하면 내 정보·연차 현황·이번 달 급여가 시드 데이터 값 그대로 표시된다 (예: 차은호/23003 → 영업1팀·대리·기본급 330만원).
6. **새로고침 후 유지**: 신청/승인 등으로 바뀐 데이터는 브라우저를 새로고침해도 그대로 유지된다 (localStorage 저장).
7. **가상 자료 표시**: 모든 화면 상단에 "⚠ 교육용 가상 자료 - 실제 정보 아님" 배너가 스크롤해도 고정되어 보인다.
8. **비품 신청 → 저장**: 일반직원이 비품을 신청하면 "대기" 상태로 저장되고, 본인의 신청 내역 목록에 바로 나타난다.
9. **비품 승인 처리 → 상태 반영**: 총무담당자가 비품 승인 화면에서 대기 건을 "승인" 처리하면, 상태가 즉시 "승인"으로 바뀌고 처리 버튼이 사라진다.
10. **대시보드 대기 건수(사번 기준)**: 승인 처리 직후, 신청자 본인이 다시 로그인하면 대시보드의 "내 대기 중인 비품 신청" 건수가 정확히 줄어든다.
11. **비품 승인 화면 접근 차단**: 일반직원/인사담당자가 "비품 승인 처리" 화면에 URL로 직접 들어가면 대시보드로 자동 이동하고, 사이드바 메뉴에도 "비품 승인 처리"가 보이지 않는다.

## 6. 다음 작업 (Known Gaps)

- [x] 로그인/데이터 매칭 로직을 이름 기준 → **사번(empNo) 기준**으로 전환 (완료 — 시드 데이터 버전 `TRAINING_SEED_VERSION`도 3으로 올려 기존 브라우저 데이터를 새 구조로 재시딩)
- [x] 훈련 참가자가 바뀔 때 데이터를 초기 상태로 되돌리는 "가상 데이터 초기화" 기능 추가 (완료 — 로그인 화면(`training_index.html`) 하단 버튼, `trainingResetAllData()`가 localStorage를 지우고 시드를 다시 채움. 확인 버튼(confirm)으로 실수 방지)

## 7. 참고 자료

- [`레퍼런스.md`](./레퍼런스.md) — flex·Frappe HR·Odoo 공식 자료에서 참고한 기능/화면 3가지
- [`데이터안내.md`](./데이터안내.md) — 직원·휴가·급여 데이터 항목 설명 및 실제 생성한 가상 데이터 구성
